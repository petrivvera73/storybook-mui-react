import React from "react";
import { App } from "./App";

export default {
  title: "App/LoginComponent",
  component: App,
};

const Template = () => <App />;

export const CartAction = Template.bind({});
